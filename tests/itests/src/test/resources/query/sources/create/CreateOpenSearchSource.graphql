mutation CreateOpenSearchSource {
  createOpenSearchSource(source: {
    creds: {
      username: "admin"
      password: "admin"
    }
    endpointUrl: "https://localhost:8993/services/catalog/query"
    sourceName: "testOpenSearch"
  })
}